specVersion: '2.5'
metadata:
  name: br.com.grupoboticario.sd.materialnbsv2
type: application
resources:
  configuration:
    propertiesFileSourceEncoding: UTF-8
builder:
  resources:
    excludes:
      - '/test/**'
      - '/localService/**'
  customTasks:
    - name: webide-extension-task-updateManifestJson
      afterTask: replaceVersion
      configuration:
        appFolder: webapp
        destDir: dist
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo
      configuration:
        archiveName: materialnbsv2
        additionalFiles:
          - xs-app.json
